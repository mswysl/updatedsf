**Checkbox** — square box, lime fill + heavy tick. Filter facets, "in stock only", terms.

```jsx
<Checkbox label="In stock only" defaultChecked />
<Checkbox label="Death Metal" onChange={e => toggle(e.target.checked)} />
```
