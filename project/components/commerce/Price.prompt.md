**Price** — money in the mono price face. Pass a number (auto-formatted) and an optional `compareAt` to show the struck original + lime save stamp.

```jsx
<Price amount={38} size="lg" />
<Price amount={38} compareAt={52} />
<Price amount={120} compareAt={150} size="sm" showSave={false} />
```
