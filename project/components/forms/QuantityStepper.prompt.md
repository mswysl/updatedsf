**QuantityStepper** — − / value / + control for cart quantity. Clamps to min/max; controlled or uncontrolled.

```jsx
<QuantityStepper defaultValue={1} min={1} max={10} onChange={setQty} />
<QuantityStepper size="sm" value={qty} onChange={setQty} />
```
